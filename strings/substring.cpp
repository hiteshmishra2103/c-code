#include <bits/stdc++.h>
using namespace std;
int main(){
    string str="hitesh";
    string s=str.substr(2,4);/*it returns the substring of
    a string from index 2(included) and 4 characters after it */
    cout<<s;
    return 0;
}