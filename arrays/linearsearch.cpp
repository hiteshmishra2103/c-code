#include <iostream>
using namespace std;
int main(){
    int n;
    cin>>n;
    int arr[n];
    cout<<"type the number you want to search";
    cin>>k;
   for (int i = 0; i < ; i++)
   {
       cin>>arr[i];
   }
   for (int i = 0; i < n; i++)
   {
       if (arr[i]==k){
           cout<<i;
           break;
       }
   }
   return 0;
}